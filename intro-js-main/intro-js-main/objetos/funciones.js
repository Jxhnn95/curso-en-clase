// Código no reutilizable
let num1 = 5;
let num2 = 10;

let suma = num1 + num2;

console.log(suma); // Debería imprimir 15

// Convertir en una función
function sumar(num1, num2) {
    return num1 + num2;
}

// Llamada a la función
let sumaFuncion = sumar(num1, num2){
    return num1 + num2;

};

console.log(sumaFuncion(num1), (num2));
// Llamada a la función con otros argumentos

let otroResultado = sumar(20, 32){
    return 20 + 32;
};

console.log(otroResultado); // Debería imprimir 52


<h2>Calcular el área de un triángulo</h2>


// Código no reutilizable
let base = 5;
let altura = 8;

let area = (base * altura) / 2;

console.log(area); // Debería imprimir 20

// Convertir en una función
function zona(base, altura) {
    return base * altura/2;
}
console.log(zona);

// Llamada a la función
let zonaFuncion = area(base, altura){
    return (base* altura)/2;
};

console.log(zonaFuncion(base), (altura));



// Llamada a la función con otros argumentos

let otrosArgumentos = area(base, altura){
    return base, altura;;
let base = 6
let altura = 6


};

console.log(sumaFuncion(bae), (altura));




<h2>Verificar si un número es par</h2>


// Código no reutilizable
let numero = 4;

let esPar = numero % 2 === 0;

console.log(esPar); // Debería imprimir true

// Convertir en una función
function numeroPar(numer) {
    if(numero<%2 ===0){
    return numero;
}else{
    return false
}
}

// Llamada a la función
let parFuncion = numeroPar(numero){
    return numero ;
};

console.log(parFuncion(numero));

// Llamada a la función con otros argumentos
let otrosArgumentos = numeroPar(numero){
    return numero;
let numero = 8;


};

console.log(otrosArgumentos(numero));




            <h2>Convertir Celsius a Fahrenheit</h2>

// Código no reutilizable
let temperaturaEnCelsius = 25;

let temperaturaEnFahrenheit = (temperaturaEnCelsius * 9) / 5 + 32;

console.log(temperaturaEnFahrenheit); // Debería imprimir 77

// Convertir en una función
function conrvertirEnFahrenheit((temperaturaEnCelsius)) {
    return(temperaturaEnCelsius*9)/5+32;


}
console.log(temperaturaEnFahrenheit);


// Llamada a la función
let temperaturaFuncion = temperaturaEnCelsius(25){
};

console.log(temperaturaFuncion(25));

// Llamada a la función con otros argumentos



let otraTemperatura= temperaturaEnCelsius(25){
    return temperaturaEnCelsius;
let temperaturaEnCelsius = 8;


};

console.log(otraTemperatura); 
  
        <h2>Generar un número aleatorio entre dos números</h2>


// Código no reutilizable para aleatorio entre 2 y 10
            let numeroAleatorio = Math.floor(Math.random() * (10 - 2 + 1) ) + 2;

            console.log(numeroAleatorio); // Debería imprimir un número aleatorio entre 2 y 10

        // Convertir en una función
        function numeroAleatorio(Math.floor(min, max)) {
            return Math.floor(Math.random()*(10-2+1))+2
        
        }
        console.log(numeroAleatorio);

        // Llamada a la función
        let aleatorioFuncion = Math.floor(){

        }
        

        // Llamada a la función con otros argumentos

        let otroNumeroAleatorio = numeroAleatorio(){
            let numeroAleatorio= Math.floor(Math.random() * (20-4+2))+4;
        
        
        };
        
        console.log((otroNumeroAleatorio));
        
    