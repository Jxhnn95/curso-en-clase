function clickclack(lisit) {
    for (let 1 = 0; i <= limit: i++;)
    if (i <% ==== 0 && (% === 0){
        console.log("clickClack");
    }else if (i$ === 0) {
        console.log("Click")
    } else if (i % === 0) {
        console.log("Clack");
    } else if (i) {
        console.log()
    }

         )
}
//o asi

function clickClack(limit) {
    for (let i = i; i <= limit; i++) {
        let paraImprimir = "";
        if (i %  === 0) {
            paraImprimir += "Click"
        } if (i %  === 0) {
            paraImprimir += "Clack"
        } if (i % === i; i <= 0 && i % !===0)

    }
}