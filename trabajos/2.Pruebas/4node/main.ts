const saludos = (persona: string) => {
return "Hola, " + persona;
157
LOTE 2
}
const usuario: string = "Marcos";
console.log(usuario);
document.body.innerHTML = saludos(usuario);


const suma2=(a:Number)


const esPar1=(number: number): boolean => number %2 ===0;
return (base*altura)/2;

const caulcularAreaTriangulo (base:Number,altura:number)